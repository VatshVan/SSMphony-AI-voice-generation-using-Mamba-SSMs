--find-links https://storage.googleapis.com/jax-releases/jax_cuda_releases.html

# Core Deep Learning (PyTorch Ecosystem)
torch>=2.5.1
torchvision>=0.20.1
torchaudio>=2.5.1
# torchfsdd removed: Use torch.distributed.fsdp instead
# torchtext removed: Library is deprecated/unmaintained

# JAX Ecosystem
# Note: On native Windows, this defaults to CPU. Use WSL2 for GPU support.
jax>=0.4.30
flax>=0.10.0

# TensorFlow
# Note: Defaults to CPU-only on native Windows (post TF 2.10)
tensorflow

# Data & IO
datasets
google-cloud-storage

# Visualization & Logging
matplotlib>=3.10.0
seaborn
wandb
celluloid

# Configuration & Utils
hydra-core
omegaconf
tqdm

# Dev Tools & Formatting
jupytext>=1.16.0
ipython
black
flake8
isort
pre-commit